import React from "react";
import { createRoot } from "react-dom/client";
import CreditCardBenefitsTracker from "./CreditCardBenefitsTracker.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CreditCardBenefitsTracker />
  </React.StrictMode>
);
